[package]
name = "casino_war_part1"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
bevy = "0.16"
rand = "0.8"

# Optimize for faster compile times in development
[profile.dev]
opt-level = 1

# Enable optimizations for dependencies but not our code
[profile.dev.package."*"]
opt-level = 3